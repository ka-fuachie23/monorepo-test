{
  "name": "monorepo-test",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "version": "lerna version --no-git-tag-version -m \":bookmark: publish\""
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.1.8"
  },
  "version": "0.0.0"
}
