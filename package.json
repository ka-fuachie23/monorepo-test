{
  "name": "monorepo-test",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "version": "npx lerna version --conventional-commits --changelog-preset conventionalcommits --no-changelog",
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.1.8"
  }
}
