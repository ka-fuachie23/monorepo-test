{
  "name": "monorepo-test",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "version": "npx lerna version --conventional-commits --changelog-preset conventionalcommits --no-changelog"
  },
  "dependencies": {},
  "devDependencies": {
    "conventional-changelog-conventionalcommits": "^8.0.0",
    "lerna": "^8.1.8"
  }
}
